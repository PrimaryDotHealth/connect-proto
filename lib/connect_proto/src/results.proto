syntax = "proto3";
package primary.connect;

import "meta.proto";
import "result.proto";

message Results {
  Meta meta = 1;
  repeated Result results = 2; // List of result components and their values
}
